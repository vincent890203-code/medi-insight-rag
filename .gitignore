# 1. 系統與暫存檔 (垃圾檔案)
__pycache__/
*.py[cod]
*.so

# 2. 虛擬環境 (這是最肥大的資料夾，絕對不傳)
venv/
.venv/
env/

# 3. 環境變數與金鑰 (⚠️ 絕對機密，傳了會有資安風險)
.env

# 4. 資料庫檔案 (本機生成的索引，不需要上傳)
chroma_db/
faiss_index/

# 5. 資料夾 (PDF 屬於隱私資料，建議不要傳上去，除非是公開範例)
data/

# 6. IDE 設定檔 (VS Code 的個人設定)
.vscode/
*.code-workspace